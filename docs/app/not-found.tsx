import { NotFoundPage } from "nextra-theme-docs";

export default function NotFound() {
    return (
        <NotFoundPage content="Report the broken link">
            <h1 className="title">There is no page here</h1>
        </NotFoundPage>
    );
}
